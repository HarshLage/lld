#include<bits/stdc++.h>
using namespace std;
int main()
{
  int *ptr = new int;
  *ptr = 5;
  cout<<*ptr<<endl;

  int * arr = new int[4];
  arr[0] = 1;
  arr[1] = 4;
  *(arr+2) = 5;
  *(arr+3) = 6;

  for(int i=0;i<4;i++){
    cout<<arr[i]<<" ";
  }

  delete []arr;

  delete ptr;
  return 0; 
}
